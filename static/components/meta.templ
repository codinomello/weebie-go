package components

templ Meta() {
    @MetaCharset()
    @MetaViewport()
}

templ MetaCharset() {
    <meta charset="UTF-8">
}

templ MetaViewport() {
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
}